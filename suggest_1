1. 这次 5-28 代码本质上是“保守融合策略”

几个改动都会天然带来同一个效果：

改动	直接影响
new_obj_threshold: 0.5 → 0.8	新目标更难被补充，减少误检，但 recall 下降
fusion_mlp zero-init	初始接近 no-op，降低对原检测特征的扰动
软门控 → 硬门控 match_conf > 0.3	低置信匹配不融合，减少错误融合和 ID switch
新目标补充更保守	少补目标，FAF/IDS 下降，但 recall/mAP 容易下降
score 缩放 ×0.5	新补目标置信度降低，进一步减少 FP/FAF，但也压低 recall

所以你现在看到的现象是合理的：

IDS 更低、FAF 更低，但 Recall / mAP / NDS 下降。

这不是偶然，而是保守策略的必然结果。

2. 所以两者的差异应该这样解释

之前的 Sinkhorn Pilot 更像是：

积极融合 + 积极补充新目标

优点是 recall 更高，AMOTA 更高，mAP/NDS 损失较小。
缺点是错误补充、错误融合更多，所以 IDS 和 FAF 没有 5-28 那么低。

现在的 Sinkhorn+DetAdapt / 5-28版本 更像是：

保守融合 + 保守补充 + 强过滤

优点是 IDS、FAF、MOTA、MOTP 更好。
缺点是召回下降，mAP/NDS/AMOTA 反而不如 Pilot。

3. 基于 baseline，哪个更好？

综合小论文说服力，我仍然建议主线用 Sinkhorn Pilot。

原因是它更平衡：

指标	Sinkhorn Pilot	5-28版本
mAP	0.0426	0.0399
NDS	0.0473	0.0450
AMOTA	0.1119	0.1101
Recall	0.220	0.211
IDS ↓	61	42
FAF ↓	47.4	41.0

5-28 的 IDS 和 FAF 更漂亮，但它是用更强的保守过滤换来的。对于论文主线来说，只强调 IDS/FAF 会显得偏后处理，检测指标下降会被审稿人抓住。

更稳的定位是：

Sinkhorn Pilot 作为主方法；5-28 保守版本作为消融实验。

4. 论文里可以这样写

可以把 5-28 版本命名为：

Conservative Fusion Variant
或者 Conservative Sinkhorn Fusion

然后解释：

在进一步引入硬门控和保守新目标补充策略后，模型显著降低 IDS 与 FAF，但由于新目标补充被抑制，Recall、mAP 和 AMOTA 有所下降。这说明过强的保守融合虽然有利于减少错误关联，但会牺牲协同感知中的有效目标补充能力。因此，本文最终采用性能更均衡的 Sinkhorn Pilot 作为主方法。

5. 下一步最值得跑的消融

为了把 5-28 的下降救回来，可以只调这三个参数：

参数	当前	建议 sweep
new_obj_threshold	0.8	0.6 / 0.7
match_conf gate	0.3	0.2 / 0.25 / 0.3
score scale	0.5	0.7 / 0.8 / 1.0

最推荐先跑：

new_obj_threshold = 0.7
match_conf_gate = 0.25
score_scale = 0.7

目标是：

mAP ≥ 0.042
AMOTA ≥ 0.112
IDS ≤ 50
FAF ≤ 50
Recall ≥ 0.218

这样就可能同时保住 Pilot 的 mAP/Recall，又接近 5-28 的 IDS/FAF。